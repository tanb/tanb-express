import { ReverseRouteDirective } from './reverse-route.directive';

describe('ReverseRouteDirective', () => {
  it('should create an instance', () => {
    const directive = new ReverseRouteDirective();
    expect(directive).toBeTruthy();
  });
});
